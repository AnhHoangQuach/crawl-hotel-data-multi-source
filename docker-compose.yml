services:
  api:
    build: .
    image: crawl-traveloka:latest
    # .env is optional: only needed when overriding HOST/PORT/OUTPUT_DIR.
    env_file:
      - path: .env
        required: false
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
